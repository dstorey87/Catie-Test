## Changelog
- New `help.html`: "How to use Theory Trainer", a guide to every feature as it works today — sign in / create account / resend confirmation / reset password, learners on one account, Home, Today's lesson (how the drill is built), Keeps tripping you up and memory tips, Practise (topics, smart mix, build your own, Focus Drill), mock tests (kinds, flags, review grid, pass mark), Flagged, My answers and More like this, Road signs, My Progress (how readiness is worked out, topic lights, coach), levels and badges, Print, every Settings option, reminders, notes, offline and syncing, installing on iPhone/Android/computer, subscription, and a part for the parent/admin (dashboard, Activity, Memory tips, question editor, packs, free access, account tools).
- 29 screenshots of the real app at 390px (example learner), in `help/img/`; each opens full size when tapped.
- An FAQ of 15 questions, answered only from what the code and docs actually do, and a "Not affiliated with the DVSA" and accuracy section.
- Draft legal pages: `legal/privacy.html` (every server table incl. `events` and the new age and guardian-consent columns, who can read what, the outside services, retention, how to get a copy or have data deleted — including the server's new export and delete-my-account functions, whose app buttons are not built yet), `legal/terms.html`, `legal/cookies.html` (the app sets no cookies; every browser-storage key listed), `legal/refunds.html`. Each is marked "Draft — not yet in force", kept out of search engines, and leaves business name, address, contact, lawful basis, retention periods and legal choices as "[to be filled in by Darren]".
- The pages look like the app (its fonts and colours) and follow the app's dark mode: the device setting, or the app's saved choice in `tt.theme`, and they switch live if the app changes it in another tab. Prices, the free-sample size and the age rule (`age.guardianUnder`) are filled from `config.js`.
- Nothing in the app links to the pages yet — the App/UI section does that (#7).
- `tests/help-pages.test.js` (18 tests): the theme and config behaviour of `help/site.js`; every link, anchor and image resolves; every screenshot has alt text and its true size; every page links back to the app with the `%20` kept and carries the DVSA disclaimer; the legal drafts contain no invented email, phone or postcode. It also fails if the pages drift from the app: a new Settings switch, a price change, a changed coach rule, a new database table, a new storage key, a new outside website, or a colour that isn't the app's own dark value.

## Requirements
- REQUIREMENTS.md §8 line "Privacy policy, terms of service, cookie/consent notice, refund policy" → 🟡 (drafts written from the code and schema; business details, lawful basis, retention periods and a legal review are Darren's)
- REQUIREMENTS.md §8 line "\"Not affiliated with DVSA\" disclaimer, and accuracy/liability wording" → 🟡 (on the guide and every legal page; not yet shown inside the app — #7; liability limits left for Darren)
- REQUIREMENTS.md §8 line "Support: contact route, FAQ, response expectation" → 🟡 (FAQ done in `help.html#faq`; contact route and response time are placeholders)
- REQUIREMENTS.md §8 line "GDPR: data export, deletion, lawful basis, processor list (Supabase, Stripe), retention policy" → 🟡 (processor list, data held and current retention documented in `legal/privacy.html`; the server's export and delete functions (#4) are described, the app buttons (#9), lawful basis and retention periods are not done)
- REQUIREMENTS.md §5 line "Refund policy and a self-serve refund request path" → 🟡 (draft policy in `legal/refunds.html`; no self-serve path)

## Status
- How-to guide: `help.html` (live at https://dstorey87.github.io/Catie-Test/help.html once promoted). Screenshots in `help/img/`, shared look in `help/site.css` and `help/site.js`. To refresh screenshots after a UI change: drive the app with `tests/browser/harness.js` at 390px and replace the file of the same name; `node --test` fails if an image's size or alt text is wrong or a file is unused.
- Legal drafts: `legal/privacy.html`, `legal/terms.html`, `legal/cookies.html`, `legal/refunds.html`. Not in force. Left for Darren: business name, address, contact email, lawful basis, retention periods, Supabase region, transfers, age/consent, liability, governing law, refund rules, and a legal review.
- Test: `node --test` (tests/help-pages.test.js). Browser-checked at 390px and 1280px, light and dark (device setting and the app's saved `tt.theme`), on a local server.
- Found while taking the screenshots, filed as #12 (App/UI lane, not fixed here): a new screen opens at the previous screen's scroll position; My Progress and the dashboard's topic list are wider than a 390px phone; the readiness number in the My Progress dial never shows (an HTML span inside SVG text); the notes button can cover "Next →" in a mock test. The guide notes the dial problem; remove that note when #12 is fixed.
